-- NOTE: Execute this AFTER creating auth user through Supabase Auth UI or API
-- Update the UUID below with the actual super admin user ID from Supabase Auth

-- Template for creating superadmin user profile (execute after auth signup)
-- Replace 'YOUR_SUPERADMIN_UUID' with the actual user ID from Supabase Auth
-- Example:
-- INSERT INTO public.users (id, email, name, role, verified)
-- VALUES ('YOUR_SUPERADMIN_UUID', 'superadmin@skillvault.com', 'Super Admin', 'super-admin', true);

-- Step-by-step instructions are provided below
